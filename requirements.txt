fastapi==0.111.0
uvicorn[standard]==0.30.1
websockets==12.0
pydantic==2.8.2
pydantic-settings==2.3.4
scapy==2.5.0
aiofiles==24.1.0

# --- Detection engine (src/detection/) ---
# numpy is pinned <2 for numba/llvmlite compatibility, which SHAP requires.
numpy==1.26.4
scikit-learn==1.5.1
xgboost==2.1.1
joblib==1.4.2

# --- Dataset assembly + metrics harness (scripts/) ---
pandas==2.2.2
pyarrow==17.0.0

# Only required for "full" mode
confluent-kafka==2.4.0
opensearch-py==2.6.0
