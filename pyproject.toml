[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "-q --strict-markers"
markers = [
    "slow: tests that run a full simulation (deselect with '-m \"not slow\"')",
]
filterwarnings = [
    "ignore::DeprecationWarning:opensearchpy.*",
]
